# Ciel — dependências principais
# Instale com: pip install -r requirements.txt

requests>=2.28
rich>=13.0
pymupdf>=1.23
pyperclip>=1.8
prompt_toolkit>=3.0
ddgs>=9.14.4
beautifulsoup4>=4.15.0
lxml>=6.1.1
simpleeval>=1.0

# ── opcional: interface TUI ───────────────────────────────────────────────────
# Necessário apenas para: python ciel.py --tui
# textual>=0.80
# pynvml              # monitoramento de VRAM (GPU) no painel da TUI
