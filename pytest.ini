[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short
markers =
    slow: testes que requerem Ollama ou conexão de rede (pule com -m "not slow")
    integration: testes de integração completa com mock de modelo
